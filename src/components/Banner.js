const Banner = () => {
    return (
    <main>
        <header>
            <h1>Orbit Report</h1>
        </header>
        <p>
           Click on the buttons to see the satellites in that orbit type
        </p>
    </main>
    );
};

export default Banner;